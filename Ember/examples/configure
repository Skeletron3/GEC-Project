# General command
./configure \
--compiler=/usr/bin/g++ \
--target=x86_64-pc-linux-gnu \
--bash \
--version=1.0-1 \
--add-makefile \
--add-config \
--add-scripts


./configure --compiler=/usr/bin/g++ --target=x86_64-pc-linux-gnu --bash --version=1.0-1 --add-makefile --add-config --add-scripts